#include "instructResponse_thread.h"
#include "serial_thread.h"

void instructResponse_thread(void *param)
{
    while (1)
    {
        // if(command_flag.current_zero = 1)
        // {
        // }
        osDelay(1000);
    }
}